//Screen 3

public class Setup_Budget
{
    public static void main(String[]args)
    {
        //Variables to hold category names and percents allocated to them.
        //Get input from GUI.
        int maxcategories = 20;
        private String[] categories = new String[20];
        private double[] percents = new double[20];

        //Checks to see if percentages add up to more than 100
        double sum = 0;
        for (i = 0; i < maxcategories; i++)
        {
            double sum += percents[i];
        }

    }
}